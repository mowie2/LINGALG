#include "../include/CollisionDetector.h"

bool CollisionDetector::Collision(Shape& first, Shape& second)
{
	bool x = false;
	bool y = false;
	bool z = false;


	
	if(x && y && z)
	{
		return true;
	}
	return false;
}
